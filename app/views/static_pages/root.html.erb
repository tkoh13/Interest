<% if logged_in? %>
  <script type="text/javascript">
    window.currentUser = {
    "id": <%= current_user.id %>,
    "email": "<%= current_user.email %>",
    "username": "<%= current_user.username %>",
    "boards": "<%= current_user.boards %>",
    "pins": "<%= current_user.pins %>",
    "photoUrl": "<%= url_for(current_user.photo).html_safe %>"
    };
  </script>
<% end %>



<main id="root">root static page - something broke</main> 
<div id="portal"></div> 

